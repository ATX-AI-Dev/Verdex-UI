{
  "name": "verdex-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Verdex-UI — design system & bibliothèque de composants multi-framework.",
  "type": "module",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "dev:vue": "pnpm --filter example-vue dev",
    "clean": "pnpm -r exec rm -rf dist node_modules/.vite"
  },
  "license": "MIT"
}
